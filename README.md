# DeepSearch
DeepSearch - Windows file search tool. Native folder picker + keyword search scans subfolders, displays numbered results with sizes using custom formatting. Select by number for instant full path. Smart truncation, safe inputs, repeat until '404'. Fast file location without manual browsing.
